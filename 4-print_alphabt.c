#include <stdio.h>

int main () {
	   char ch;

	      for(ch = 'a' ; ch <= 'z' ; ch++) {
		      if (ch=='e') {
			      ch++;
		      } else if (ch=='q') {
			      ch++;
		      } else {
			      putchar(ch);
	         
	         return(0);
}
